# 开发环境配置
spring:
  mail:
    host: smtp.qq.com
    port: 465
    username: ${MAIL_USERNAME:1519939545@qq.com}
    password: ${MAIL_PASSWORD:neosiexgnwgjgbjb}
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
    from-address: ${MAIL_FROM_ADDRESS:1519939545@qq.com}
    from-name: ${MAIL_FROM_NAME:学生核心能力培养系统}
  datasource:
    url: jdbc:mysql://localhost:3306/student_assessment_system?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: Ab*83327000
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
  
  # redis:
  #   host: localhost
  #   port: 6379
  #   database: 0
  
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none

# MyBatis（确保 dev 下也能解析 mapper 文件）
mybatis:
  mapper-locations: classpath*:mapper/*.xml

# 开发环境日志配置
logging:
  level:
    com.noncore.assessment: INFO
    root: WARN

application:
  base-url: http://localhost:5173
  email:
    default-language: zh-CN

# 开发环境文件上传配置
file:
  upload-dir: ./uploads
  max-size: 50MB
  allowed-extensions: jpg,jpeg,png,gif,pdf,doc,docx,xls,xlsx,ppt,pptx,txt,zip,rar,mp4,mov,avi,mkv,webm
  purpose-max-sizes: course_material=1GB,course_video=1GB
  
# 开发环境JWT配置
jwt:
  secret: dev-jwt-secret-key-for-development-environment-should-be-secure
  expiration: 86400000  # 24小时 

# 开发环境默认走 OpenRouter（OpenAI 兼容）。密钥请用环境变量 DEEPSEEK_API_KEY 注入。
ai:
  default-provider: openrouter
  providers:
    openrouter:
      base-url: https://openrouter.ai/api
      api-key: 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
      referer: https://stucoreai.space
      title: StudentCoreAI
    google:
      base-url: https://generativelanguage.googleapis.com
      api-key: AIzaSyAbK4-_QLcaWwmADkdxG4xNBb_J61XYhtY
  deepseek:
    model: z-ai/glm-4.5-air:free
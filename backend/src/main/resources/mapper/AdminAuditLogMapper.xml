<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.noncore.assessment.mapper.AdminAuditLogMapper">

    <insert id="insert" parameterType="com.noncore.assessment.entity.AdminAuditLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO admin_audit_logs (
            actor_id, action, target_type, target_id, detail_json, ip, user_agent, created_at
        ) VALUES (
            #{actorId}, #{action}, #{targetType}, #{targetId}, #{detailJson}, #{ip}, #{userAgent}, NOW()
        )
    </insert>

</mapper>


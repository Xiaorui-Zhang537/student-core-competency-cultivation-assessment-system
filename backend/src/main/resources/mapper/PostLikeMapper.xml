<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.noncore.assessment.mapper.PostLikeMapper">

    <!-- 点赞字段映射 -->
    <resultMap id="BaseResultMap" type="com.noncore.assessment.entity.PostLike">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="post_id" property="postId" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 基础字段列表 -->
    <sql id="Base_Column_List">
        id, post_id, user_id, created_at
    </sql>

    <!-- 插入点赞记录 -->
    <insert id="insert" parameterType="com.noncore.assessment.entity.PostLike" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO post_likes (post_id, user_id)
        VALUES (#{postId}, #{userId})
    </insert>

    <!-- 删除点赞记录 -->
    <delete id="delete">
        DELETE FROM post_likes 
        WHERE post_id = #{postId} AND user_id = #{userId}
    </delete>

    <!-- 查询用户是否已点赞 -->
    <select id="selectByPostIdAndUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM post_likes
        WHERE post_id = #{postId} AND user_id = #{userId}
    </select>

    <!-- 查询帖子的点赞用户列表 -->
    <select id="selectUserIdsByPostId" resultType="long">
        SELECT user_id
        FROM post_likes
        WHERE post_id = #{postId}
        ORDER BY created_at DESC
    </select>

    <!-- 查询用户点赞的帖子列表 -->
    <select id="selectPostIdsByUserId" resultType="long">
        SELECT post_id
        FROM post_likes
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <!-- 统计帖子的点赞数 -->
    <select id="countByPostId" resultType="long">
        SELECT COUNT(*)
        FROM post_likes
        WHERE post_id = #{postId}
    </select>

    <!-- 统计用户的点赞数 -->
    <select id="countByUserId" resultType="long">
        SELECT COUNT(*)
        FROM post_likes
        WHERE user_id = #{userId}
    </select>

    <!-- 批量查询用户对帖子的点赞状态 -->
    <select id="selectLikedPostIds" resultType="long">
        SELECT post_id
        FROM post_likes
        WHERE user_id = #{userId}
        AND post_id IN
        <foreach collection="postIds" item="postId" open="(" separator="," close=")">
            #{postId}
        </foreach>
    </select>

</mapper> 
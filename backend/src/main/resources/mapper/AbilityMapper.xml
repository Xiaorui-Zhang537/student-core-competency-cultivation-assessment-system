<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.noncore.assessment.mapper.AbilityMapper">

    <resultMap id="AbilityResultMap" type="com.noncore.assessment.entity.Ability">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="category" property="category"/>
        <result column="level" property="level"/>
        <result column="dimension_id" property="dimensionId"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="abilityColumns">
        id, name, description, category, level, dimension_id, created_at, updated_at, deleted
    </sql>

    <insert id="insertAbility" parameterType="com.noncore.assessment.entity.Ability">
        INSERT INTO abilities (id, name, description, category, level, dimension_id, created_at, updated_at, deleted)
        VALUES (#{id}, #{name}, #{description}, #{category}, #{level}, #{dimensionId}, #{createdAt}, #{updatedAt}, #{deleted})
    </insert>

    <update id="updateAbility" parameterType="com.noncore.assessment.entity.Ability">
        UPDATE abilities
        SET
            name = #{name},
            description = #{description},
            category = #{category},
            level = #{level},
            dimension_id = #{dimensionId},
            updated_at = #{updatedAt}
        WHERE id = #{id} AND deleted = false
    </update>

    <update id="deleteAbilityById">
        UPDATE abilities
        SET deleted = true, updated_at = NOW()
        WHERE id = #{abilityId}
    </update>

    <select id="selectAbilityById" resultMap="AbilityResultMap">
        SELECT <include refid="abilityColumns"/>
        FROM abilities
        WHERE id = #{abilityId} AND deleted = false
    </select>

    <select id="selectAllAbilities" resultMap="AbilityResultMap">
        SELECT <include refid="abilityColumns"/>
        FROM abilities
        WHERE deleted = false
        ORDER BY created_at DESC
    </select>

    <select id="selectAbilitiesByDimensionId" resultMap="AbilityResultMap">
        SELECT <include refid="abilityColumns"/>
        FROM abilities
        WHERE dimension_id = #{dimensionId} AND deleted = false
        ORDER BY name
    </select>

    <insert id="batchInsertAbilities" parameterType="java.util.List">
        INSERT INTO abilities (id, name, description, category, level, dimension_id, created_at, updated_at, deleted)
        VALUES
        <foreach collection="abilityList" item="ability" separator=",">
            (#{ability.id}, #{ability.name}, #{ability.description}, #{ability.category}, #{ability.level}, #{ability.dimensionId}, #{ability.createdAt}, #{ability.updatedAt}, #{ability.deleted})
        </foreach>
    </insert>

    <select id="searchAbilitiesByName" resultMap="AbilityResultMap">
        SELECT <include refid="abilityColumns"/>
        FROM abilities
        WHERE name LIKE CONCAT('%', #{name}, '%') AND deleted = false
        ORDER BY name
    </select>

</mapper> 
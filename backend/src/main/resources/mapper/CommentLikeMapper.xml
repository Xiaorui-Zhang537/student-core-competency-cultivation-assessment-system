<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.noncore.assessment.mapper.CommentLikeMapper">

    <resultMap id="CommentLikeResultMap" type="com.noncore.assessment.entity.PostComment">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="comment_id" property="commentId"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO comment_likes (user_id, comment_id, created_at)
        VALUES (#{userId}, #{commentId}, NOW())
    </insert>

    <delete id="delete">
        DELETE FROM comment_likes
        WHERE user_id = #{userId} AND comment_id = #{commentId}
    </delete>

    <select id="countLikesByCommentId" resultType="int">
        SELECT COUNT(*)
        FROM comment_likes
        WHERE comment_id = #{commentId}
    </select>

    <select id="selectByCommentIdAndUserId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM comment_likes
        WHERE user_id = #{userId} AND comment_id = #{commentId}
    </select>

    <select id="findUsersWhoLikedComment" resultType="long">
        SELECT user_id
        FROM comment_likes
        WHERE comment_id = #{commentId}
    </select>
</mapper> 
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.noncore.assessment.mapper.AbilityGoalMapper">

    <!-- 结果映射 -->
    <resultMap id="AbilityGoalResultMap" type="com.noncore.assessment.entity.AbilityGoal">
        <id column="id" property="id"/>
        <result column="student_id" property="studentId"/>
        <result column="dimension_id" property="dimensionId"/>
        <result column="title" property="title"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="target_score" property="targetScore"/>
        <result column="deadline" property="deadline"/>
        <result column="completed_at" property="completedAt"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <resultMap id="AbilityGoalDetailResultMap" type="com.noncore.assessment.entity.AbilityGoal" extends="AbilityGoalResultMap">
        <association property="dimension" javaType="com.noncore.assessment.entity.AbilityDimension">
            <id column="dimension_id" property="id"/>
            <result column="dimension_name" property="name"/>
        </association>
        <association property="student" javaType="com.noncore.assessment.entity.User">
            <id column="student_id" property="id"/>
            <result column="student_name" property="displayName"/>
        </association>
    </resultMap>

    <!-- 基础查询字段 -->
    <sql id="abilityGoalColumns">
        g.id, g.student_id, g.dimension_id, g.title, g.description, g.status,
        g.target_score, g.deadline, g.completed_at, g.created_at, g.updated_at, g.deleted
    </sql>

    <sql id="abilityGoalDetailColumns">
        <include refid="abilityGoalColumns"/>,
        d.name as dimension_name,
        u.display_name as student_name
    </sql>

    <!-- 插入能力目标 -->
    <insert id="insertGoal" parameterType="com.noncore.assessment.entity.AbilityGoal" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ability_goals (
            student_id, dimension_id, title, description, status,
            target_score, deadline, created_at, updated_at, deleted
        ) VALUES (
            #{studentId}, #{dimensionId}, #{title}, #{description}, #{status},
            #{targetScore}, #{deadline}, NOW(), NOW(), false
        )
    </insert>

    <!-- 更新能力目标 -->
    <update id="updateGoal" parameterType="com.noncore.assessment.entity.AbilityGoal">
        UPDATE ability_goals
        <set>
            <if test="dimensionId != null">dimension_id = #{dimensionId},</if>
            <if test="title != null">title = #{title},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="targetScore != null">target_score = #{targetScore},</if>
            <if test="deadline != null">deadline = #{deadline},</if>
            <if test="completedAt != null">completed_at = #{completedAt},</if>
            updated_at = NOW()
        </set>
        WHERE id = #{id} AND g.deleted = false
    </update>

    <!-- 软删除能力目标 -->
    <update id="deleteGoal" parameterType="long">
        UPDATE ability_goals
        SET deleted = true, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 根据学生ID查询所有目标 -->
    <select id="selectGoalsByStudentId" parameterType="long" resultMap="AbilityGoalDetailResultMap">
        SELECT <include refid="abilityGoalDetailColumns"/>
        FROM ability_goals g
        LEFT JOIN ability_dimensions d ON g.dimension_id = d.id
        LEFT JOIN users u ON g.student_id = u.id
        WHERE g.student_id = #{studentId} AND g.deleted = false
        ORDER BY g.created_at DESC
    </select>

    <!-- 根据学生ID查询活跃目标 -->
    <select id="selectActiveGoalsByStudent" parameterType="long" resultMap="AbilityGoalDetailResultMap">
        SELECT <include refid="abilityGoalDetailColumns"/>
        FROM ability_goals g
        LEFT JOIN ability_dimensions d ON g.dimension_id = d.id
        LEFT JOIN users u ON g.student_id = u.id
        WHERE g.student_id = #{studentId} AND g.status != 'ACHIEVED' AND g.deleted = false
        ORDER BY g.deadline ASC
    </select>

    <!-- 根据学生ID和维度ID查询活跃目标 -->
    <select id="selectActiveGoalsByStudentAndDimension" resultMap="AbilityGoalDetailResultMap">
        SELECT <include refid="abilityGoalDetailColumns"/>
        FROM ability_goals g
        LEFT JOIN ability_dimensions d ON g.dimension_id = d.id
        LEFT JOIN users u ON g.student_id = u.id
        WHERE g.student_id = #{studentId} AND g.dimension_id = #{dimensionId} AND g.status != 'ACHIEVED' AND g.deleted = false
        ORDER BY g.deadline ASC
    </select>

    <!-- 批量更新目标状态 -->
    <update id="batchUpdateGoalStatus">
        UPDATE ability_goals
        SET status = #{status}, updated_at = NOW()
        WHERE id IN
        <foreach collection="goalIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- 获取即将到期的目标 -->
    <select id="selectUpcomingDeadlineGoals" resultMap="AbilityGoalDetailResultMap">
        SELECT <include refid="abilityGoalDetailColumns"/>
        FROM ability_goals g
        LEFT JOIN ability_dimensions d ON g.dimension_id = d.id
        LEFT JOIN users u ON g.student_id = u.id
        WHERE g.student_id = #{studentId}
          AND g.status != 'ACHIEVED'
          AND g.deleted = false
          AND g.deadline BETWEEN NOW() AND DATE_ADD(NOW(), INTERVAL #{days} DAY)
        ORDER BY g.deadline ASC
    </select>

    <!-- 获取已达成的目标数量 -->
    <select id="countAchievedGoals" parameterType="long" resultType="integer">
        SELECT COUNT(1)
        FROM ability_goals g
        WHERE g.student_id = #{studentId} AND g.status = 'ACHIEVED' AND g.deleted = false
    </select>

</mapper> 